# URL base del backend FastAPI (sin barra final).
VITE_API_URL=http://127.0.0.1:8000/api/v1

# Modo demo del Módulo 2: "true" = log y versiones simulados, sin backend.
# Ausente o cualquier otro valor = false (usa el backend real).
VITE_MODO_MOCK=false
